export enum ProjectStatusConstant {
  ACTIVE = 0,
  INACTIVE = 1,
  COMPLETED = 2,
  CANCELED = 3,
}

export enum TaskStatusConstant {
  ACTIVE = 0,
  INACTIVE = 1,
}

export enum StatusConstant {
  PENDING = 0,
  APPROVE = 1,
  REJECT = 2,
}